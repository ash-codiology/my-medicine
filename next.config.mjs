/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['th.bing.com', 'www.bing.com', 'img.freepik.com'], // Add the domains of your external images here
      },
      
};

export default nextConfig;
